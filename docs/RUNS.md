# Run ledger

Reference runs, newest last. Summaries only — `store/` artifacts stay out
of git (each entry names the command to reproduce). Versions apply per
entry.

<!-- Job 04 fills the two M0 exit-gate entries below with real run data
     before merge; this file is created by that job. -->
